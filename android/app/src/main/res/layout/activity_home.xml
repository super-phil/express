<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.magic.express.activity.HomeActivity"
    android:weightSum="1">


    <ListView
        android:id="@+id/home_list_view"
        android:layout_width="match_parent"
        android:layout_height="200sp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true" />

    <Button
        android:text="扫码录入"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/home_button_scan"
        android:layout_above="@+id/button_group"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        tools:ignore="HardcodedText" />

    <LinearLayout
        android:id="@+id/button_group"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true">

        <Button
            android:text="快递列表"
            android:layout_width="wrap_content"
            android:layout_height="60sp"
            android:layout_weight="1"
            android:id="@+id/home_button_list"
            tools:ignore="ButtonStyle,HardcodedText" />

        <Button
            android:text="常用备注"
            android:layout_width="wrap_content"
            android:layout_height="60sp"
            android:layout_weight="1"
            android:id="@+id/home_button_remark"
            tools:ignore="ButtonStyle,HardcodedText" />

        <Button
            android:text="待开发中"
            android:layout_width="wrap_content"
            android:layout_height="60sp"
            android:id="@+id/home_button_other"
            android:layout_weight="1"
            tools:ignore="ButtonStyle,HardcodedText" />
    </LinearLayout>

</RelativeLayout>
